# Phase 0 / 0.5 local scripts — same pins as requirements.txt (minus FastAPI).
# Prefer installing backend/requirements.txt for the full API.
torch==2.2.2
torchvision==0.17.2
transformers==4.44.2
pillow==10.4.0
timm==1.0.9
numpy==1.26.4
